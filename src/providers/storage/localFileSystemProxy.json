{
    "title": "${strings.connections.providers.local.title}",
    "required": [
        "folderPath"
    ],
    "type": "object",
    "properties": {
        "folderPath": {
            "title": "${strings.connections.providers.local.folderPath}",
            "type": "string"
        }
    }
}