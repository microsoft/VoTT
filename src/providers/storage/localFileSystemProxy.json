{
    "title": "${connections.providers.local.title}",
    "required": [
        "folderPath"
    ],
    "type": "object",
    "properties": {
        "folderPath": {
            "title": "${connections.providers.local.folderPath}",
            "type": "string"
        }
    }
}