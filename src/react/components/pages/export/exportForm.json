{
    "type": "object",
    "properties": {
        "providerType": {
            "title": "${common.provider}",
            "type": "string",
            "enum": [
                "vottJson",
                "azureCustomVision",
                "tensorFlowRecords"
            ],
            "default": "vottJson",
            "enumNames": [
                "${export.providers.vottJson}",
                "${export.providers.azureCV}",
                "${export.providers.tfRecords}"
            ]
        },
        "providerOptions": {
            "type": "object"
        }
    }
}