{
    "type": "object",
    "properties": {
        "providerType": {
            "title": "${common.provider}",
            "type": "string",
            "enum": [
                "vottJson",
                "azureCustomVision",
                "tensorFlowRecords"
            ],
            "default": "vottJson",
            "enumNames": [
                "${exportPage.providers.vottJson}",
                "${exportPage.providers.azureCV}",
                "${exportPage.providers.tfRecords}"
            ]
        },
        "providerOptions": {
            "type": "object"
        }
    }
}