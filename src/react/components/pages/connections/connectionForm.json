{
    "title": "${connections.title}",
    "required": [
        "name",
        "providerType"
    ],
    "type": "object",
    "properties": {
        "name": {
            "title": "${common.displayName}",
            "type": "string"
        },
        "description": {
            "title": "${common.description}",
            "type": "string"
        },
        "providerType": {
            "title": "${common.provider}",
            "type": "string"
        }
    }
}